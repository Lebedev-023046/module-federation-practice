## 📦 Микрофронтенд-проект с Vue, React, NestJS и PostgreSQL

Этот проект — демонстрация архитектуры микрофронтендов, где Vue и React работают как независимые remote-приложения, подключаемые в host-приложение через Module Federation.
Также в репозитории есть полноценный backend на NestJS + Prisma + PostgreSQL, подключённый через Docker.

---

## 🗂 Структура проекта

```
.
├── backend/ # NestJS + Prisma + PostgreSQL
│ ├── src/
│ ├── prisma/
│ ├── Dockerfile
│ ├── docker-compose.yml
│ └── .env
│
├── frontend/ # Микрофронтенды и host-приложение
│ ├── host/ # Host-приложение (React)
│ ├── react-app/ # Remote React-приложение
│ └── vue-app/ # Remote Vue-приложение
│
└── README.md

```

---

## 🚀 Как запустить приложение

1. Клонируем репозиторий

```bash
	git clone <repo-url>
	cd project-name
```

2.  Запускаем backend

    2.1. Создаём .env файл в backend/  
    2.2. Добавляем переменные окружения по примеру из .env.example:

    - POSTGRES_USER
    - POSTGRES_PASSWORD
    - POSTGRES_DB
    - DATABASE_URL

      2.3

3.  Запускаем React remote приложение

    3.1. Создаём .env файл в frontend/react-app  
    3.2. Добавляем переменные окружения по примеру из .env.example:

    - VITE_API_URL

```bash
	cd frontend/react-app
	pnpm i
	pnpm run serve
```

4.  Запускаем Vue remote приложение.

    4.1. Создаём .env файл в frontend/react-app  
     4.2. Добавляем переменные окружения по примеру из .env.example:

    - VITE_API_URL

```bash
	cd frontend/vue-app
	pnpm i
	pnpm run serve
```

5. Запускаем Host приложение

```bash
	cd frontend/host
	pnpm i
	pnpm run dev
```
